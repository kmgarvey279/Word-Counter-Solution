@{
Layout = "_Layout";
}

@using WordCounter.Models;

@if (Model.Count == 0)
{
  <p>You haven't checked any words yet!</p>
}
@if (Model.Count !=0)
{
  @foreach (CheckScore checkscore in Model)
  {
  <li>Word: @checkscore.GetUserInput()<br>
      Scrabble Score: @checkscore.GetUserScore()
  </li>
  }
}

</ul>
<a href="/scrabblescore/new">Check a new word</a>
